import Layout from "@/components/Layout";
import { UserSettings } from "@protoxyz/auth-ui";

function Page() {
    return (
        <Layout>
            <UserSettings />
        </Layout>
    );
}

export default Page;
